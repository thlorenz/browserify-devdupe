{
  "name": "depends-0.1.0",
  "version": "0.0.0",
  "description": "depends on common module like bar",
  "main": "depends-0.1.0.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
