{
  "name": "depends-1.0.0",
  "version": "0.0.0",
  "description": "depends on common module like bar",
  "main": "depends-1.0.0.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
